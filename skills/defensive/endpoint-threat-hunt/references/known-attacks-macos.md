# Known Targeted Attacks - macOS

> Concise pointers to named campaigns targeting macOS. Match hunt output against these high-signal IOCs.
> **These rotate.** Hashes/domains go stale fast; LaunchDaemon/LaunchAgent labels, bundle ids, dropped filenames, and behavioral patterns are more durable. Always confirm against the linked source before calling a `❌ FAIL`.

| Campaign (aliases) | Type | High-signal IOCs (defanged) | Source |
|---|---|---|---|
| **Atomic macOS Stealer** (AMOS) | Infostealer + persistent backdoor, via cracked apps, fake installer `.dmg`, and `curl \| bash` terminal lures | **Persistence (LaunchDaemon):** `/Library/LaunchDaemons/com.finder.helper.plist` → runs `.agent` via `/bin/bash` at boot<br>**Hidden drop files (home dir):** `~/.helper` (main binary), `~/.agent` (loop script), `~/.pass` (captured password), `~/.username`<br>**Behavior:** `osascript` fake "System Preferences/System Settings" password dialog; Keychain export; browser cred harvest (Chrome/Firefox/Safari/Edge/Brave/Opera/Vivaldi); staged as `/tmp/out.zip`; crypto-wallet targeting<br>**Delivery filenames:** `Installer_v.X.XX.dmg` (e.g. `v.2.13`, `v.3.89`, `v.7.26`)<br>**C2 / landing:** `ekochist[.]com`, `misshon[.]com`, `toutentris[.]com`; install scripts `goatramz[.]com/get4/install.sh`, `letrucvert[.]com/get8/install.sh`; exfil `sivvino[.]com/contact`, `45[.]94[.]47[.]149`, `45[.]94[.]47[.]186`<br>**Detections:** `Trojan.MacOS.Amos.PFH`, `TrojanSpy.MacOS.AMOS.MANP`<br>**Variant - masquerades as Apple daemons (Objective-See):** LaunchAgents `com.apple.systemupdate` / `com.apple.mdworker` (`~/Library/LaunchAgents/com.apple.mdworker.plist`); drops `~/.pwd`, `~/.marker`, `/tmp/helper`, `/tmp/archive.tar.gz`; C2 `laislivon[.]com`, `rvdownloads[.]com/frozenfix/update`; 256 hardcoded wallet-extension IDs (e.g. MetaMask `nkbihfbeogaeaoehlefnkodbefgpgknn`)<br>_Persistence label/path varies per build (`com.finder.helper` vs. Apple-masquerading names) - match on any._ | [Trend Micro, 2025-09](https://www.trendmicro.com/en_us/research/25/i/an-mdr-analysis-of-the-amos-stealer-campaign.html) · [Objective-See, 2026-04](https://objective-see.org/blog/blog_0x88.html) |
| **CrashStealer** | Notarized-dropper infostealer (passes Gatekeeper) | **Notarized dropper:** `Werkbit.app` from `werkbit[.]io` (registered 2026-06), Developer ID **Emil Grigorov (WWB7JA7AQV)**, gated behind a meeting PIN<br>**Second stage:** `sys.cache` pulled from `github.com/mgothiclove` → `CrashReporter.dmg` → executes `veltod` in `/tmp`<br>**Persistence:** LaunchAgent; unlocks Keychain with the validated local password<br>**Exfil:** ZIP archive to `179.43.166[.]242`<br>**Targets:** ~80 crypto wallets, 14 password managers, Chromium browsers, `~/Documents`, `~/Downloads` | [The Hacker News, 2026-07-13](https://thehackernews.com/2026/07/crashstealer-macos-malware-uses.html) |
| **Fake Maccy Stealer** | Infostealer masquerading as the Maccy clipboard app (SEO poisoning) | **Persistence (Login Items, not LaunchAgent):** registers via `LSSharedFileList` / `SMAppService` APIs<br>**Bundle id:** `com.apple.finder.monitor`<br>**Install paths:** `~/Library/Application Support/com.apple.finder.monitor/Finder.app/Contents/MacOS/`, `~/Library/Caches/com.apple.finder.monitor/`, `~/Library/HTTPStorages/com.apple.finder.monitor/`<br>**SHA256:** `45bd0e32…384c48c9`<br>**Delivery:** `maccyapp[.]com` (fake DMG, obfuscated JXA)<br>**C2:** `avengerflow[.]com/api/sync` (`172.67.210.219`, `104.21.93.138`; origin `51.92.110.33` AWS) | [ManageEngine, 2026-06-30](https://www.manageengine.com/products/desktop-central/blog/fake-maccy-stealer.html) |
| **Sapphire Sleet** (DPRK state actor) | Targeted crypto-theft; fake-recruiter `.scpt` lures → `curl`-to-`osascript` backdoor chain | **Lure files (`.scpt`):** `Zoom SDK Update.scpt`, `MSTeams SDK Update.scpt` (opened in Script Editor)<br>**Drop files:** `~/.zoom.log` (infection marker), `~/Library/Services/services` (backdoor), `~/Library/Application Support/iCloud/icloudz`, `~/Library/Google/com.google.chromes.updaters`, `/tmp/lg4err`<br>**Persistence:** LaunchDaemon `com.google.webkit.service.plist`; LaunchAgent `com.apple.identification.plist`<br>**Fake dialogs:** `systemupdate.app` / `softwareupdate.app` password popups<br>**C2:** `uw04webzoom[.]us`, `check02id[.]com`, `termsliva[.]com`; `188.227.196[.]252:443`, `83.136.208[.]246:6783`<br>**Exfil:** curl UA strings `mac-cur1`…`mac-cur5`; ZIPs `tapp_<user>.zip`, `ext_<user>.zip`, `exds_<user>.zip` | [Microsoft, 2026-04-16](https://www.microsoft.com/en-us/security/blog/2026/04/16/dissecting-sapphire-sleets-macos-intrusion-from-lure-to-compromise/) |

<!--
Add a row per campaign. Keep IOC cells to the most distinctive, low-false-positive indicators
(LaunchDaemon/LaunchAgent labels, bundle ids, dropped filenames, Login Items, osascript behavior) - link the source for the full list.
Table rows must be contiguous (no blank lines between them) or Markdown breaks the table.
-->
